# Local environment for running the test suite and the offline analysis
# (experiments/fidelity_sweep/analysis/analyze.py + docs/figures regeneration).
#
# The CLOUD experiment images pin their own dependencies separately and do NOT
# use this file — see experiments/fidelity_sweep/modal_pkg/fidelity_modal.py
# (Modal image) and experiments/fidelity_sweep/kernel_pkg/fidelity_kernel.py
# (Kaggle install step). Those pin torch==2.4.1+cu121, transformers==5.3.0, etc.
#
# Python >= 3.10. Versions below are the ones this repo was tested with; relax
# to >= if exact wheels are unavailable for your platform.
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
torch==2.12.0
pytest==9.0.3
