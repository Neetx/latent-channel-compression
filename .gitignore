# Python
__pycache__/
.pytest_cache/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
.env
.envrc

# Large model artifacts (never commit)
**/*.safetensors
**/*.bin
**/*.pt
**/*.ckpt
**/*.npz
**/*.h5
**/*.gguf

# Upstream code clones (user re-clones via README instructions)
external/

# Captured experiment outputs (re-generated by running scripts)
experiments/results/

# Per-machine secrets — NEVER commit
.kaggle/
.mcp.json
.claude/settings.local.json
*.env

# Third-party workspace-local agent skills (installed tooling, not project artifacts)
.claude/skills/

# Jupyter
.ipynb_checkpoints/

# OS / editors
.DS_Store
.idea/
.vscode/
*.swp
*~

# Logs
*.log
wandb/

# arXiv submission bundle (regenerate with writeup/make_arxiv_bundle.sh)
writeup/arxiv_submission/
writeup/arxiv_submission.tar.gz

# LaTeX build artifacts (writeup/) — main.pdf is kept intentionally
*.aux
*.bbl
*.blg
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
